<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Appender para consola -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender para archivo -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>target/logs/serenity.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>target/logs/serenity.%i.log</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>10</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>10MB</maxFileSize>
        </triggeringPolicy>
    </appender>

    <!-- Logger para Serenity - WARN level -->
    <logger name="net.serenitybdd" level="WARN"/>
    <logger name="net.thucydides" level="WARN"/>

    <!-- Logger para Selenium - WARN level -->
    <logger name="org.openqa.selenium" level="WARN"/>

    <!-- Logger para Photography/Darkroom - WARN level -->
    <logger name="net.serenitybdd.core.photography" level="WARN"/>
    <logger name="net.serenitybdd.screenplay.targets" level="WARN"/>
    <logger name="net.thucydides.core.steps" level="WARN"/>

    <!-- Logger para tu código - INFO level -->
    <logger name="inetum" level="INFO"/>

    <!-- Root logger -->
    <root level="WARN">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
